{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "unset VIRTUAL_ENV; export PYTHONWARNINGS=ignore::SyntaxWarning; uv run --project \"${CLAUDE_PLUGIN_ROOT}\" python \"${CLAUDE_PLUGIN_ROOT}/alhazen_core.py\" init",
            "timeout": 90000
          }
        ]
      }
    ]
  }
}
